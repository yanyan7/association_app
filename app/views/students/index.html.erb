<h1>生徒一覧</h1>
<table>
  <tr>
    <th>id</th>
    <th>name</th>
  </tr>
  <% @students.each do |student|%>
    <tr>
      <td><%= student.id %></td>
      <td><%= link_to student.name, student %></td>
    </tr>
  <% end %>
</table>